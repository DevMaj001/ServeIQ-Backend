{
  "name": "serveiq",
  "version": "1.0.0",
  "description": "ServeIQ — Hospitality Operations Platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:api": "npm run start:dev -w apps/api",
    "lint": "npm run lint -w apps/api",
    "format": "prettier --write \"apps/**/*.{ts,md}\""
  },
  "dependencies": {
    "@sentry/nestjs": "^10.67.0",
    "@sentry/node": "^10.68.0"
  },
  "devDependencies": {
    "@types/multer": "^2.1.0",
    "husky": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
